import 'package:flutter/material.dart';

class NewPublication extends StatelessWidget {
  const NewPublication({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Crear publicación'),
      ),
      body: Center(
        child: Text('Crear publicación'),
      ),
    );
  }
}
